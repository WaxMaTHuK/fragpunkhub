"use client"; import { useState } from "react"; import { LockKeyhole, Zap } from "lucide-react";
export function AuthClient(){const [mode,setMode]=useState<"login"|"register">("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false); async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError("");const r=await fetch("/api/player-auth",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({mode,email,password})});const d=await r.json().catch(()=>({}));setLoading(false);if(r.ok)location.href="/profile";else setError(d.error||"Не удалось войти");}return <main className="auth-page"><section className="auth-card"><a className="brand" href="/"><span className="brand-mark"/><span className="brand-text"><strong>FRAGPUNK</strong><small>HUB.RU</small></span></a><div className="auth-icon"><LockKeyhole/></div><p className="eyebrow">Кабинет игрока</p><h1>{mode==="login"?"Войти":"Создать аккаунт"}</h1><p className="auth-copy">Сохраняй прогресс, уровни, коллекцию и достижения на сайте.</p><form onSubmit={submit}><label>Почта<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Пароль<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Минимум 8 символов"/></label>{error&&<p className="auth-error">{error}</p>}<button disabled={loading}><Zap size={18}/>{loading?"Подождите…":mode==="login"?"Войти":"Зарегистрироваться"}</button></form><p className="auth-switch">{mode==="login"?"Нет аккаунта? ":"Уже есть аккаунт? "}<button onClick={()=>{setMode(mode==="login"?"register":"login");setError("")}}>{mode==="login"?"Регистрация":"Войти"}</button></p></section></main>}